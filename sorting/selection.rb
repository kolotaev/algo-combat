#
# Selection sort is the most trivial sort.
# Time complexity is O(n^2).
# Actually (n^2)/2.
#
# Applications:
# - only for small sets as a brute force approach.
#
class Selection
  def sort(data)
    n = 0
    (0..data.size).each do |i|
      min = data[n]
      (i..data.size).each do |j|

      end
    end
    data
  end
end
